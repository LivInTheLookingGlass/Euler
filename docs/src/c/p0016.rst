C Implementation of Problem 16
==============================

View source code :source:`c/src/p0016.c`

Includes
--------

- `"bcd.h" <./lib/bcd.html>`__
- `"macros.h" <./macros.html>`__ (implicitly via bcd.h)
- `"math.h" <./math.html>`__ (implicitly via bcd.h & if compiled on PCC)
- :cref:`\<inttypes.h\> <types/integer>`
- :cref:`\<stdint.h\> <types/integer>`
- :cref:`\<stdio.h\> <io>`
- :cref:`\<stdbool.h\> <types>` (implicitly via bcd.h)
- :cref:`\<string.h\> <string/byte>` (implicitly via bcd.h)
- :cref:`\<stdlib.h\> <program>` (implicitly via bcd.h & if not compiled on PCC)
- :cref:`\<math.h\> <numeric/math>` (implicitly via bcd.h & if not compiled on PCC)

Solution
--------

.. c:function:: uint64_t p0016()

.. c:function:: int main(int argc, char const *argv[])

   .. note:: 
      
      This function is only present in the Python test runner, or when compiling as a standalone program.
      It is not present when compiling for the Unity test runner.

.. literalinclude:: ../../../c/src/p0016.c
   :language: C
   :linenos:

.. tags:: large-numbers, digit-sum, power
